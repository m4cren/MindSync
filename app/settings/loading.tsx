import React from "react";

const loading = () => {
   return (
      <div className=" flex items-center justify-center w-full h-screen">
         <span className="loading loading-infinity loading-xl scale-300" />
      </div>
   );
};

export default loading;
